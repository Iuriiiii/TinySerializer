# 0.0.19

Enable encoders and decoders.

# 0.0.16

Enhance `classSerializer` and `classDeserializer`
Make optional `serializer` member on `SerializableClass` abstraction
Add new static method for `SerializableClass` classes called `deserialize`

# 0.0.16

Enhance `numberSerializer` and `numberDeserializer` to support numbers from -(2\**53) to (2**53)

# 0.0.15

Better `stringSerializer` and `stringDeserializer`, export `NumberSerializationType`

# 0.0.10

Empty array deserialization

# 0.0.10

Fix `stringDeserializer`

# 0.0.9

Better database constructor and fixed array references

# 0.0.8

Remove bytes argument from number deserializers

# 0.0.7

Add `Latest`value to `Opcode` enum

# 0.0.6

Rename `Serialization` enum to `Opcode`

# 0.0.5

Export enums correctly

# 0.0.4

Export `NumberSerializationType` and `mergeBuffers`

# 0.0.3

Add serialize and deserialize classes

# 0.0.2

Add serialize and deserialize functions

# 0.0.1

Add native datatypes serialization
